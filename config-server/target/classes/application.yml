server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///C:/Users/Limani/config-repo

logging:
  level:
    org.springframework.cloud.config: DEBUG